Одно замечание по entropy — мой комментарий про H(m) vs H(m|context) не учтён. План по-прежнему мерит просто H(messages) без учёта контекста. Это значит агент, который случайно чередует 10 сообщений (шум), получит точно такой же entropy bonus, как агент с идеальным языком (10 разных сообщений для 10 разных контекстов). При коэффициенте 1.0 и с cap ±1.0 это не катастрофа, но стоит иметь в виду как tech debt — на следующей итерации переделать на conditional metric.
Одно новое, чего не было: _cic_speaker_pending с orig_x, orig_y — это решает мою проблему B про «слушатель уже шёл к рифту». Записывая позицию до сообщения, можно сравнить direction before vs after. Хорошее дополнение, но в описании не сказано как именно используется orig_x/orig_y — убедись что в _resolve_cic_events() реально проверяется distance_to_rift_before > distance_to_rift_after, а не просто «listener находится ближе к рифту».
Entropy cap ±1.0 — это правильно, я не упоминал это, но это хорошая страховка от доминирования entropy signal.
В целом — это чистый, деплой-ready план. Можно давать «y» в Claude Code.